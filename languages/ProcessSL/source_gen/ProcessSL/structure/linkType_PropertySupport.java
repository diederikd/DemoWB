package ProcessSL.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class linkType_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<linkType> constants = ListSequence.fromList(linkType.getConstants()).iterator();
    while (constants.hasNext()) {
      linkType constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }
  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<linkType> constants = ListSequence.fromList(linkType.getConstants()).iterator();
    while (constants.hasNext()) {
      linkType constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }
  public String fromInternalValue(String value) {
    linkType constant = linkType.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "";
  }
}
