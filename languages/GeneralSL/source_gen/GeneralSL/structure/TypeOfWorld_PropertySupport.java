package GeneralSL.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class TypeOfWorld_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<TypeOfWorld> constants = ListSequence.fromList(TypeOfWorld.getConstants()).iterator();
    while (constants.hasNext()) {
      TypeOfWorld constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }
  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<TypeOfWorld> constants = ListSequence.fromList(TypeOfWorld.getConstants()).iterator();
    while (constants.hasNext()) {
      TypeOfWorld constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }
  public String fromInternalValue(String value) {
    TypeOfWorld constant = TypeOfWorld.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "production | coordination";
  }
}
